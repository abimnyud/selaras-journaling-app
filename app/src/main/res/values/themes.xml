<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SelarasJournalingApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="Theme.SelarasJournalingApp" parent="Base.Theme.SelarasJournalingApp" />

    <style name="Theme.SearchBar" parent="Theme.SelarasJournalingApp">
        <item name="android:editTextColor">@color/mutedPrimaryForeground</item>
        <item name="android:textColorHint">@color/mutedPrimaryForeground</item>
    </style>
</resources>